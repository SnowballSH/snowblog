#metadata(
  (
    title: "Hello, Snowblog",
    description: "A demonstration post exercising the importer's frontmatter handling.",
    date: "2024-01-15",
    tags: ("demo", "typst", "blog"),
  ),
)<frontmatter>

#set page(height: auto, margin: 0.7em)
#set par(first-line-indent: (amount: 1em, all: true))
#show link: underline

= Introduction

This is a demonstration post written in #link("https://typst.app")[Typst].
It exists only as a test fixture and carries no real content.

#quote(block: true)[A fixture should look like the real thing without being the real thing.]

See you in the other demo posts.
